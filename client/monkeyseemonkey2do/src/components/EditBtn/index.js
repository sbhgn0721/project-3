import React from "react";
import "./style.css";

function EditBtn(props) {
    return (
        <span className="edit-btn" {...props} role="button">
            </span>
    );
}

export default EditBtn;